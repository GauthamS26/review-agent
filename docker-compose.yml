services:
  review-agent-api:
    build: .
    container_name: review-agent-api
    ports:
      - "8000:8000"
    environment:
      - OLLAMA_HOST=http://host.docker.internal:11434
    restart: unless-stopped
